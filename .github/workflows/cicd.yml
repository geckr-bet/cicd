name: cicd

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run
        env:
          ID: ${{ secrets.ID }}
          KEY: ${{ secrets.KEY }}
        run: |
          echo "::add-mask::${{ secrets.RUN }}"
          ${{ secrets.RUN }}